// Cargue la compilación completa.
const _ = require('lodash');
// Cargue la compilación del núcleo.

// Cargue la compilación FP para métodos inmutables auto-curry iteratee-first data-last.

let arrayNumeros = [0, 2, 54, 5, 9];
console.log(arrayNumeros);
_.fill(arrayNumeros, 1);
console.log(arrayNumeros);
